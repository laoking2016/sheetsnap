# 阶段 1：核心链路 — 验证标准

> 所有任务完成后，依次执行以下验证。全部通过则此阶段可合并至 `main`。

---

## 验证 1：文件上传

| # | 检查项 | 操作 | 预期结果 |
|---|--------|------|---------|
| V1.1 | 上传按钮可见 | 打开首页 | "Upload Quotation File" 大按钮可见 |
| V1.2 | 点击上传 | 点击按钮 → 选择 .xlsx 文件 | 文件选择器弹出，仅显示允许的文件类型 |
| V1.3 | 拖拽上传 | 拖拽 .xlsx 文件到页面上 | 文件被接收，开始上传 |
| V1.4 | 文件类型校验 | 上传 .txt 或 .jpg 文件 | 提示 "请上传 Excel 或 PDF 格式的报价单" |
| V1.5 | 文件大小校验 | 上传 >10MB 的文件 | 提示 "文件大小不能超过 10MB" |
| V1.6 | API 返回 200 | `curl -F "file=@test.xlsx" http://localhost:3000/api/upload` | 返回 JSON，`success: true` |

## 验证 2：文档解析

| # | 检查项 | 操作 | 预期结果 |
|---|--------|------|---------|
| V2.1 | Excel 解析 | 上传一个包含表格的 .xlsx 报价单 | 返回行列数据，非空 |
| V2.2 | PDF 解析 | 上传一个表格式 PDF 报价单 | 返回行列数据，非空 |
| V2.3 | CSV 解析 | 上传一个 CSV 报价单 | 返回行列数据，非空 |
| V2.4 | 空文件 | 上传空的 Excel 文件 | 返回 `success: false`，提示 "未能识别出表格" |
| V2.5 | 无表格文件 | 上传纯文本 PDF（无表格） | 返回 `success: false`，提示 "未能识别出表格" |

## 验证 3：列映射

| # | 检查项 | 操作 | 预期结果 |
|---|--------|------|---------|
| V3.1 | 标准列存在 | 解析任何报价单 | 输出包含全部 5 个标准列 |
| V3.2 | 产品名称映射 | 上传包含 "Product Name" 列的表格 | 列正确映射到 "产品名称" |
| V3.3 | 单价映射 | 上传包含 "Unit Price" / "单价" / "Price" 的表格 | 正确映射到 "单价" |
| V3.4 | 货币识别 | 上传包含 "$", "USD", "CNY" 的表格 | 正确识别货币列 |
| V3.5 | 最小起订量 | 上传包含 "MOQ", "Min Order", "最小起订量" 的表格 | 正确映射 |
| V3.6 | 跳过标题行 | 上传含 2-3 行标题/LOGO 的报价单 | 自动跳过非数据行 |
| V3.7 | 3 个真实样本 | 用 3 个不同格式的真实报价单测试 | 成功率 ≥ 80% |

## 验证 4：单位归一化

| # | 检查项 | 操作 | 预期结果 |
|---|--------|------|---------|
| V4.1 | "pcs" 归一化 | 报价单含 "pcs" | 输出 "个" |
| V4.2 | "kg" 归一化 | 报价单含 "kg" | 输出 "公斤" |
| V4.3 | 未知单位 | 报价单含 "lbs" | 保留原文 "lbs"，标记 ⚠️ |
| V4.4 | 覆盖率 | 用 10 个常见英文单位测试 | 至少 8/10 被正确归一化 |

## 验证 5：CSV 生成 + 下载

| # | 检查项 | 操作 | 预期结果 |
|---|--------|------|---------|
| V5.1 | CSV 可下载 | 解析成功后 | "Download CSV" 按钮可见 |
| V5.2 | 编码正确 | 用 `file` 命令检查 | `charset=utf-8` |
| V5.3 | BOM 存在 | `xxd \| head -1` | 以 `feff` 开头 |
| V5.4 | Excel 打开不乱码 | 用 Excel 打开生成的 CSV | 中文正常显示，列对齐 |
| V5.5 | 文件名正确 | 下载文件 | `原始文件名_sheetsnap.csv` |

## 验证 6：端到端

| # | 检查项 | 操作 | 预期结果 |
|---|--------|------|---------|
| V6.1 | 完整流程 | 上传 → 预览 → 下载 | 整个流程 < 10 秒 |
| V6.2 | 未登录用户 | 不登录直接上传 | 解析成功后弹出邮箱收集对话框 |
| V6.3 | 警告展示 | 解析有无法映射的单元格 | ⚠️ 图标显示，悬浮可看原文 |

---

## 合并条件

- [ ] V1.1 – V1.6 (上传 ✅)
- [ ] V2.1 – V2.5 (解析 ✅)
- [ ] V3.1 – V3.7 (列映射 ✅)
- [ ] V4.1 – V4.4 (单位归一化 ✅)
- [ ] V5.1 – V5.5 (CSV ✅)
- [ ] V6.1 – V6.3 (端到端 ✅)
